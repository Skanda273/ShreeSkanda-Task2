# Task 2 Backend Environment Variables
PORT=5000
JWT_SECRET=task2_super_secret_jwt_key_2026

# PostgreSQL Connection Configuration (Optional - falls back to In-Memory DB if not set)
# DATABASE_URL=postgres://username:password@localhost:5432/task2_db
PGUSER=postgres
PGPASSWORD=postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=task2_db
